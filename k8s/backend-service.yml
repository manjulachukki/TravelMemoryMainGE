apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: travelmemory
  labels:
    app: travelmemory
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: travelmemory
    tier: backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
